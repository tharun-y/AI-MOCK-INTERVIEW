{
  "name": "iiit-sricity---2",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npx kill-port 5000 && cross-env NODE_ENV=development nodemon backend/server.js",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npx kill-port 5000 && cross-env NODE_ENV=production node backend/server.js"
},

  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.11.0",
    "nodemon": "^3.1.9"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "kill-port": "^2.0.1"
  }
}
